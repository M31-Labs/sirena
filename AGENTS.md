# AGENTS

Sirena is the diagram language for the mdpp ecosystem. Canonical spec, plan, decisions, and concepts live at `hypha://m31labs/sirena`. Use `hypha recall sirena` for context before non-trivial work. Commits go through `buckley commit --yes --minimal-output` — never `git commit`. Plan files and spec files NEVER live in this repo; they live in the hyphae space.
